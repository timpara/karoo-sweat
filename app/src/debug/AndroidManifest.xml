<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          Developer harness, debug builds only. Renders the real data field with
          synthetic inputs so layout can be checked without a Karoo. Never shipped
          in a release build.
        -->
        <activity
            android:name=".ui.HarnessActivity"
            android:exported="true"
            android:label="Sweat harness">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
